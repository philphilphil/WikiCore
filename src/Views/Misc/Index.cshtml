@model WikiCore.Models.MiscModel

@{
  ViewData["Title"] = "Miscellaneous";
}
<h2>Tags</h2>
<h4>Remove</h4>
<form method="post" asp-action="DeleteTag" asp-controller="Misc" onsubmit="return confirm('Tag and all connections to pages will be irreversible removed.');"> 
  <div class="ui labeled input">
  <div class="ui label">
    Tag
  </div>
  <select asp-for="TagToDeleteId"  asp-items="@Model.TagsSelect" class="ui dropdown"></select>
</div>
 <button type="submit" class="ui primary button">Remove</button>
Tag will be deleted and removed from all pages.
</form>

<h2>All pages:</h2>
 @foreach (var page in Model.Pages) {
     <p>      
        @Html.RouteLink(page.Title, "Page", new { id = page.PageId })
     </p>
 }

 <h2>Version</h2>
 v1.0 - 19.03.2017